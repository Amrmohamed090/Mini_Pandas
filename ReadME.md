first have a general explaination, say that this implemenation is inspired from Pandas (most famous library to process csv's in python)

explain 1- how I auto detect the datatype,
2- the structure of the two classes
- we store everything in a colomaner format
- to query the dataset, the most suitable apporach I thought about is using a binary mask. this allows using operations for complex queries like & | ~ etc. (give an example of how it makes things easier)
- the use of bitset, the drawback (fixed-size), in a future work, we can mitigate it by using dynamic bitset


give example usage


document each function

